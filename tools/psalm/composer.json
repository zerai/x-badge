{
    "require-dev": {
        "psalm/plugin-phpunit": "^0.18.0",
        "vimeo/psalm": "^5.11"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
