{
    "require-dev": {
        "psalm/plugin-phpunit": "^0.18.0",
        "vimeo/psalm": "^4.6"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
