{
    "require-dev": {
        "psalm/plugin-phpunit": "^0.18.0",
        "vimeo/psalm": "^5.0"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
