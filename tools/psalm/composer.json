{
    "require-dev": {
        "psalm/plugin-phpunit": "^0.18.0",
        "vimeo/psalm": "^5.1"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
